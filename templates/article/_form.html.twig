{{ form_start(form) }}
    {{ form_widget(form) }}
    <input name="user_id" id="user_id" value="{{ app.user.id }}">
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
