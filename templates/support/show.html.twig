{% extends 'public/layout.html.twig' %}

{% block title %}Support{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-md-center">
            <div class="col-auto mt-4">

                <div class="card text-center card_article_show">
                    <div class="card-header">
                        <h3>{{ support.title }}</h3>
                    </div>
                    <div class="card-body overflow-auto  card_article" >
                        {# <h5 class="card-title">Special title treatment</h5> #}
                        <p class="card-text">{{ support.information|nl2br }}</p>
                    </div>
                    <div class="card-footer text-muted ">
                        <p class="card-text">{{ support.user.name  }}</p>
                        <a tybe="button" class="btn btn-secondary d-inline-block" href="{{ path('admin_gestion_user_article') }}">Retour</a>
                    </div>
                </div>

                {# <a href="{{ path('article_edit', {'id': article.id}) }}">edit</a> #}

                {# {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_SUPER_ADMIN') %} 
                    <div class="mt-2">
                        {{ include('article/_delete_form.html.twig') }}       
                    </div>               
                {% endif %} #}
            
            </div>
        </div>
    </div>

    
{% endblock %}
