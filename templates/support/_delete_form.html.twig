<form method="post" action="{{ path('support_delete', {'id': support.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="d-inline-block">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ support.id) }}">
    <button class="btn btn-secondary">Resolu</button>
</form>
