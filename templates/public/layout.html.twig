
{% extends 'base.html.twig' %}


{% block body %}
        
{% endblock %}


{% block nav %}


            {% if app.user.getThemeSombre() == true %}
                {% set check = "checked" %}
                {% set nav = "navbar-dark bg-dark" %}
                {% set color = "#ffffff8c" %}
            {% else %}
                {% set check = "" %}
                {% set nav = "navbar-light bg-light" %}
            {% endif %}
        
            {# <nav class="navbar navbar-expand-lg {{ nav }}" id="nav">
                <div class="container-fluid">
                    <a class="navbar-brand agora-nav" href="{{ path('article_index') }}">Agora</a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0 ">
                

                     

                     

                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_logout') }}">
                                <i class="fas fa-sign-out-alt fa-2x"></i>Deconnexion
                            </a>
                        </li>

                        <li class="nav-item">
                            <div class="form-check form-switch">
                                <input class="form-check-input" type="checkbox" id="theme" {{check}}>
                            </div>
                        </li>

                    </ul>
                    <form class="d-flex">

                        
                       
                        
                        <i class="bi bi-box-arrow-right"></i>
                    </form>
                    </div>
                </div>
            </nav> #}


        <ul>
            <li class="d-inline">
                <a href="{{ path('article_index') }}">
                    <i class="fas fa-home fa-2x"></i>Agora
                </a>
            </li>
            <li class="d-inline">
                <a href="{{ path('article_user') }}">
                    <i class="fas fa-newspaper fa-2x"></i> Article
                </a>
            </li>
            <li class="d-inline">
                <a href="{{ path('user_signet') }}">
                    <i class="fas fa-bookmark fa-2x"></i> Signet
                </a>
            </li>
            <li class="d-inline">
                <a href="{{ path('user_edit') }}">
                    <i class="fas fa-user fa-2x"></i> Profil
                </a>
            </li>
            <li class="d-inline">
                <div class="form-check form-switch " style="margin-top: 30px; font-size: 20px">
                    <input class="form-check-input " type="checkbox" id="theme" {{check}}>
                </div>
            </li>
            <li class="d-inline" style="float:right">
                {% if is_granted('ROLE_ADMIN') %} 
                    <a href="{{ path('admin_gestion_user_article') }}">
                        <i class="fas fa-tools fa-2x"></i>Admin
                    </a>
                {% endif %}
            </li>

            <li  class="d-inline" style="float:right">
                <a href="{{ path('app_logout') }}">
                    <i class="fas fa-sign-out-alt fa-2x"></i>Deconnexion
                </a>
            </li>

           
        </ul>

            
            


            

{% endblock %}

{% block footer %}

    {% if app.user.getThemeSombre() == true %}
        {% set footerBackgroundColor = "background-color: #212529" %}
        {% set footerColor = "color: white" %}
    {% else %}
        {% set footerBackgroundColor = "background-color: white" %}
        {% set footerColor = "color: black" %}
    {% endif %}


    <div class="text-center p-3 footer" style="{{ footerBackgroundColor}} ; {{ footerColor }}">      
        © 2021 Copyright:
        <a class="text-white">Agora.com</a>
    </div>
{% endblock %}
